const NotFound = () => {
  return <div>The requested page doesn@apos;t exist....</div>
}
export default NotFound

// explore (later) / revise - this does not get rendered at localhost
// but only gets rendered at pages other than/inside localhost - mentioned in config.matcher
